{
  "name": "@horva/cli",
  "version": "0.0.1",
  "type": "module",
  "bin": {
    "horva": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "node build.mjs",
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "dev": "node build.mjs --watch",
    "format": "prettier --check . --ignore-path ../../.gitignore",
    "lint": "eslint",
    "pack": "node build.mjs && cd dist && pnpm pack --pack-destination ..",
    "typecheck": "tsc"
  },
  "prettier": "@horva/prettier-config",
  "dependencies": {
    "@horva/core": "workspace:*",
    "@horva/db": "workspace:*",
    "@inquirer/prompts": "^7.5.0",
    "chalk": "^5.4.1",
    "commander": "^14.0.3",
    "dotenv": "catalog:",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@horva/eslint-config": "workspace:*",
    "@horva/prettier-config": "workspace:*",
    "@horva/tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "@types/pg": "^8.20.0",
    "esbuild": "^0.28.0",
    "eslint": "catalog:",
    "prettier": "catalog:",
    "typescript": "catalog:"
  }
}
